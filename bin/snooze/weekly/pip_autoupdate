#!/bin/sh
cd "$HOME/.local/share/python/bin" ||
    exit
for i in *
do
    pip install "$i" --upgrade
done

# find "$HOME/.local/share/python/bin" -type f -print0 |
#     xargs -I {} -0 pip install {} --upgrade
