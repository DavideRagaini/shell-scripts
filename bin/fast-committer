#!/bin/sh

base="$HOME/.local/src/"
# cd "$base/<++>" && git pull && [ -n "$(git status)" ] && git pull && nvim -c MagitOnly && git push
cd "$base/dotfiles" && git pull && nvim -c MagitOnly && git push
cd "$base/dwm" && git pull && nvim -c MagitOnly && git push
cd "$base/shell-scripts" && git pull && nvim -c MagitOnly && git push
cd "$base/startpage" && git pull && nvim -c MagitOnly && git push
cd "$base/wiki" && git pull && nvim -c MagitOnly && git push
cd "$base/nvim" && git pull && nvim -c MagitOnly && git push
cd "$base/dwmblock" && git pull && nvim -c MagitOnly && git push
cd "$base/st" && git pull && nvim -c MagitOnly && git push
cd "$base/dmenu" && git pull && nvim -c MagitOnly && git push
# cd "$base/<++>" && git pull && [ -n "$(git status)" ] && git pull && nvim -c MagitOnly && git push
