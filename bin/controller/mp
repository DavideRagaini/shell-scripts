#!/bin/sh

[ -n "$(pidof spotifyd)" ] &&
	CHOSEN="Spotify" ||
	CHOSEN="$(printf 'Spotify\nLocal' | fzf --cycle --border --margin 10% --padding 10% --header='Choose music source')"

case "$CHOSEN" in
Spotify)
	spotifyd &&
		spotify-tui
	;;
Local)
	ncmpcpp
	;;
esac
